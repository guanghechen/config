# {{name}}

gui:
  authorColors:
    '*': '{{unified.fg2}}'
    guanghechen: '{{unified.blue}}'
  branchColorPatterns:
    "^(main|master)$": '{{unified.purple}}'
    "^(user/[^/]+/)?chore": '{{unified.fg2}}'
    "^(user/[^/]+/)?feat": '{{unified.green}}'
    "^(user/[^/]+/)?feature": '{{unified.green}}'
    "^(user/[^/]+/)?fix": '{{unified.red}}'
    "^(user/[^/]+/)?refactor": '{{unified.aqua}}'
    "^(user/[^/]+/)?release": '{{unified.blue}}'
  theme:
    activeBorderColor:
      - '{{unified.aqua}}'
      - bold
    cherryPickedCommitBgColor:
      - '{{rosepine.overlay}}'
    cherryPickedCommitFgColor:
      - '{{unified.aqua}}'
    defaultFgColor:
      - '{{unified.fg1}}'
    inactiveBorderColor:
      - '{{rosepine.highlightHigh}}'
    inactiveViewSelectedLineBgColor:
      - '{{rosepine.surface}}'
    markedBaseCommitBgColor:
      - '{{rosepine.highlightMed}}'
    markedBaseCommitFgColor:
      - '{{unified.fg1}}'
    optionsTextColor:
      - '{{unified.blue}}'
    searchingActiveBorderColor:
      - '{{unified.yellow}}'
      - bold
    selectedLineBgColor:
      - '{{rosepine.highlightMed}}'
      - bold
    unstagedChangesColor:
      - '{{unified.red}}'

git:
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --{{darken ? 'dark' : 'light'}}
