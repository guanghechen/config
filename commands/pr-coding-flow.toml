description = "Create a PR for the current branch using coding-flow MCP tool"
prompt = """Please create a pr for the current branch by the coding-flow mcp tool. Better to gen a reasonable pr title rather than using a default one.
"""
