-- {{name}}

local config = {
  colors = {
    -- Primary colors
    background    = "{{unified.bg0}}",
    foreground    = "{{catppuccin?.text || unified.fg0}}",

    -- Cursor colors (following official catppuccin theme)
    cursor_bg     = "{{catppuccin?.rosewater || unified.pink}}",
    cursor_fg     = "{{catppuccin?.crust || unified.bg1}}",
    cursor_border = "{{catppuccin?.rosewater || unified.pink}}",

    -- Selection colors (following official catppuccin theme)
    selection_bg  = "{{catppuccin?.surface2 || unified.bg4}}",
    selection_fg  = "{{catppuccin?.text || unified.fg0}}",

    -- Split separator color
    split         = "{{catppuccin?.overlay0 || unified.fg4}}",

    -- Scrollbar thumb color
    scrollbar_thumb = "{{catppuccin?.surface2 || unified.bg4}}",

    -- Visual bell color
    visual_bell   = "{{catppuccin?.surface0 || unified.bg2}}",

    -- Compose cursor (for IME)
    compose_cursor = "{{catppuccin?.flamingo || unified.orange}}",

    -- Normal colors
    ansi = {
      "{{catppuccin?.surface1 || unified.bg3}}",  -- black (surface1 for dark themes, subtext1 for latte)
      "{{unified.red}}",
      "{{unified.green}}",
      "{{unified.yellow}}",
      "{{unified.blue}}",
      "{{catppuccin?.pink || unified.purple}}",
      "{{catppuccin?.teal || unified.aqua}}",
      "{{catppuccin?.subtext1 || unified.fg1}}",  -- white (subtext1 for dark themes, surface2 for latte)
    },

    -- Bright colors
    brights = {
      "{{catppuccin?.surface2 || unified.grey}}",  -- bright black (surface2 for dark themes, subtext0 for latte)
      "{{unified.brightRed}}",
      "{{unified.brightGreen}}",
      "{{unified.brightYellow}}",
      "{{unified.brightBlue}}",
      "{{catppuccin?.pink || unified.brightPurple}}",
      "{{catppuccin?.teal || unified.brightAqua}}",
      "{{catppuccin?.subtext0 || unified.fg2}}",  -- bright white (subtext0 for dark themes, surface1 for latte)
    },

    -- Indexed colors (extended palette)
    indexed = {
      [16] = "{{catppuccin?.peach || unified.orange}}",
      [17] = "{{catppuccin?.rosewater || unified.pink}}",
    },

    -- Tab bar colors (following official catppuccin theme)
    tab_bar = {
      background = "{{catppuccin?.crust || unified.bg1}}",

      active_tab = {
        bg_color = "{{catppuccin?.mauve || unified.purple}}",
        fg_color = "{{catppuccin?.crust || unified.bg1}}",
      },

      inactive_tab = {
        bg_color = "{{catppuccin?.mantle || unified.bg1}}",
        fg_color = "{{catppuccin?.text || unified.fg0}}",
      },

      inactive_tab_hover = {
        bg_color = "{{catppuccin?.base || unified.bg0}}",
        fg_color = "{{catppuccin?.text || unified.fg0}}",
      },

      new_tab = {
        bg_color = "{{catppuccin?.surface0 || unified.bg2}}",
        fg_color = "{{catppuccin?.text || unified.fg0}}",
      },

      new_tab_hover = {
        bg_color = "{{catppuccin?.surface1 || unified.bg3}}",
        fg_color = "{{catppuccin?.text || unified.fg0}}",
      },

      inactive_tab_edge = "{{catppuccin?.surface0 || unified.bg2}}",
    },
  },

  -- Window frame colors
  window_frame = {
    active_titlebar_bg = "{{catppuccin?.crust || unified.bg1}}",
    active_titlebar_fg = "{{catppuccin?.text || unified.fg0}}",
    inactive_titlebar_bg = "{{catppuccin?.crust || unified.bg1}}",
    inactive_titlebar_fg = "{{catppuccin?.text || unified.fg0}}",
    button_fg = "{{catppuccin?.text || unified.fg0}}",
    button_bg = "{{catppuccin?.base || unified.bg0}}",
  },

  -- Command palette colors
  command_palette_bg_color = "{{catppuccin?.crust || unified.bg1}}",
  command_palette_fg_color = "{{catppuccin?.text || unified.fg0}}",
}

return config
