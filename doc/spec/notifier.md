## Spec for notifier.

1. [x] The implementation located at @lua/eve/builtin/notifier.lua
2. [x] Dressing the default `vim.notify` (@lua/fml/dressing/notifier.lua)
2. [x] If the message body is empty, then we don't render the winbar but put the content as the message body instead.
