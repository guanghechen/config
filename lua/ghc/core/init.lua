local globals = require("ghc.core.globals")
local icons = require("ghc.core.icons")
local symbols = require("ghc.core.symbols")

local settings = {
  globals = globals,
  icons = icons,
  symbols = symbols,
}

return settings
