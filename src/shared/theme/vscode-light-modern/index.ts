import type { IThemeDefinition } from '../contract'
import { vscodeLightModernPalette } from './palette'

export const vscodeLightModernTheme: IThemeDefinition = {
  id: 'vscode-light-modern',
  label: 'VS Code Light Modern',
  kind: 'light',
  palette: vscodeLightModernPalette,
}
