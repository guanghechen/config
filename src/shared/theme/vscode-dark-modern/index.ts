import type { IThemeDefinition } from '../contract'
import { vscodeDarkModernPalette } from './palette'

export const vscodeDarkModernTheme: IThemeDefinition = {
  id: 'vscode-dark-modern',
  label: 'VS Code Dark Modern',
  kind: 'dark',
  palette: vscodeDarkModernPalette,
}
