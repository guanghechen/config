interface:
  display_name: "Review Loop"
  short_description: "Gate significant changes with iterative adversarial review"
  default_prompt: "Use $review-loop to gate the current significant change with bounded fresh adversarial reviews until Converged, clearly blocked, or user input is required."
