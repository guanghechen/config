interface:
  display_name: 'Tsuki Agent'
  short_description: 'Inspect granted pages with safe scoped capabilities'
  default_prompt:
    'Inspect the granted browser page through Tsuki and use only explicitly granted notes or
    allowlisted scroll/highlight actions.'
