interface:
  display_name: "PR Review"
  short_description: "Review a PR or base-to-worktree diff for risk, scope, value, and merge readiness"
  default_prompt: "Use $pr-review to review a specific PR, or all changes since a given base up to the current working tree, for risk, scope, value, testability, and merge readiness."
