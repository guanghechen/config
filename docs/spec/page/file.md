!!!Please use the react-engineer to handle this task.

## Task Details

create a new page on the route `/file`, the view placed on the @src/view/file/, it should be inspired from the @src/view/workspace/

1. Accept the parameter `filepath` from the search param like the @src/view/workspace/ did.
2. Only switch to different filetype view based on the filepath extension.
3. no sidebar, no topbar, no floating button, but just render the filetype view.
4. Need to maintain similar context/ and Composer.tsx, but no sidebar, no topbar, no floating button, but just render the filetype view in the Composer.
5. need to maintain few sub states of the workspace viewmodel, like the filepathDirty 

