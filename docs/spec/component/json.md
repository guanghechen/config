@src/component/json/

## string

for the string value, that's add a additional svg button side of the copy button to toggle the string display:

1. Let's named the svg button as 'prettier', you can pick or create a reasonable svg icon for it.
2. When the `prettier` enabled, let's make the `\n` rendered as line ending. maybe consider wrap into a `<pre><code>` ? I'm not sure, consider it by yourself.
