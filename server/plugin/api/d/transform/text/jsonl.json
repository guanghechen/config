{
  "name": "jsonl",
  "desc": "item => `Line ${Object.keys(item).join(', ')}`",
  "split": "text => text.trim().split(/\\n/g)",
  "uuid": "(_, index) => `${index}`",
  "parents": "() => []",
  "title": "item => ''",
  "chainPaths": [],
  "steps": [
    {
      "type": "filter",
      "code": "(line, index) => index < 33",
      "skip": true
    },
    {
      "type": "map",
      "code": "line => JSON.parse(line)",
      "skip": false
    }
  ]
}
