# Wiki / Troubleshoot 工作约定

本仓库是公开的个人知识库，用于整理参考知识、操作速查和问题排查记录。

## 何时修改仓库

- 咨询与调试默认直接回答，不主动创建或修改文件。
- 用户明确要求创建、修改、整理或归档内容时，执行相应的仓库变更。
- 优先使用用户指定的路径；未指定时按下方目录规范和已有条目归类。分类或目标位置仍不明确时，先询问再写入。

## 公开内容与隐私

写入正文、frontmatter、日志摘录、截图或配图前必须脱敏。严禁写入：

- **凭据**：API key、token、密码、私钥、连接串。
- **个人身份信息**：真实姓名、邮箱、电话及其他 PII。
- **私有环境信息**：内网 endpoint、主机名、IP、公司内部服务名、私有仓库路径。
- **本地信息**：绝对路径中的用户名等不宜公开的信息。

用明确的占位符替换真实值，例如 `<API_KEY>`、`user@example.com`、`10.x.x.x`、`/path/to/project`。脱敏范围包括附件及其 metadata。

发现疑似敏感内容时，暂停相关写入和提交并确认；可以继续处理不依赖该内容的部分。

## 目录与索引

- `cs/` — 计算机相关知识，沿用已有领域目录。
- `cs/os/<平台>/` — 操作系统相关知识，例如 `nix/`、`win/`。
- `cs/lang/<语言>/` — 编程语言相关知识，例如 `rust/`。
- `topic/<主题>/` — 其他主题的参考知识，例如 `rubiks-cube/`。
- `troubleshoot/` — 问题排查记录，文件名为 `YYYY-MM-DD-<slug>.md`。
- `README.md` — 导航索引；新增、移动或删除条目时同步更新链接。

FAQ 放在所属主题目录内，例如 `cs/os/win/faq.md`。配图放在条目所在目录的 `assets/` 中，使用相对链接引用。

## 内容与格式

- Wiki 条目至少包含一级标题与 `tags`，沿用所在目录的 frontmatter 约定。
- Troubleshoot 记录包含 frontmatter：`title`、`date`、`tags`、`env`、`status`；正文说明症状与触发条件、排查证据、根因、处理方法和验证结果。未确定的根因或未验证的结论应明确标注。
- 技术说明写清适用条件，关键结论提供来源或验证依据。公式、代码、转动记号等标识符保持准确，配图与正文一致。

## 表达与检查

- 使用简体中文，保留英文技术术语；结论优先，较长回答附 `TL;DR`。
- Markdown 表格与 ASCII 图保持视觉对齐：CJK 字符按 2 列、ASCII 字符按 1 列计算。
- 完成修改后，检查目录归属、README 索引和本地链接；移动文件时确认内容完整，旧路径引用已同步更新。
